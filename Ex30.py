def llegir():
    l=[]
    a ="a"
    while a!=".":
        a = input("Pon el nombre: ")
        if a!=".":
            l.append(a)
    return l 
def ncp(l,p):
    e=[]
    a=0
    for e in l:
        if e[0]==p:
            a+=1
            q.append(e)
print:("El numero de paraules que començen {} per a son {} i son {} " .format(p,a,q))
a=llegir()
x=input("Introdueix el caracter que vol cercar: ")
ncp(a,c)